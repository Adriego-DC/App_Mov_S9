<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.FormularioAsistencia" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Aquí van los items -->
        <item name="colorPrimary">@color/verde_claro</item>
        <item name="colorPrimaryVariant">@color/verde_oscuro</item>
        <item name="colorOnPrimary">@color/text_primary</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="colorOnBackground">@color/text_primary</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>

    </style>

    <!-- Este hereda del base -->
    <style name="Theme.FormularioAsistencia" parent="Base.Theme.FormularioAsistencia" />

</resources>
